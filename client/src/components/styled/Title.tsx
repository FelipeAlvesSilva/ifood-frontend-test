import styled from 'styled-components';
// import { Typography } from 'antd';
import { text } from '../theme/theme';

// const { Title } = Typography;

//set dynamic title size
export const Title = styled.h1`
  color: ${text} !important;
  font-weight: 900;
`;